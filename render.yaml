---
services:
  - type: web
    name: mba-backend-main
    env: python
codex/prepare-repo-for-final-render
    pythonVersion: 3.11.9

 codex/prepare-repo-for-final-render
    pythonVersion: 3.11.9

 codex/prepare-repo-for-final-render
    pythonVersion: 3.11.>>>>>>> main
 main
       main
    plan: starter
    region: oregon
    branch: main
    buildCommand: ./render-build.sh
    startCommand: >-
    codex/prepare-repo-for-final-render

 codex/prepare-repo-for-final-render
     main
      bash -c "if [ -f /opt/render/project/.venv/bin/activate ]; then
        source /opt/render/project/.venv/bin/activate
      fi
      exec gunicorn -k uvicorn.workers.UvicornWorker -w 4 -b 0.0.0.0:8000 backend.main:app"
       codex/prepare-repo-for-final-render


      bash -c "source /opt/render/project/.venv/bin/activate &&
      gunicorn -k uvicorn.workers.UvicornWorker -w 4 -b 0.0.0.0:8000 backend.main:app"
 main
      main
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: DATABASE_URL
        fromService:
          name: mba-postgres-main
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: mba-redis
          property: connectionString
     codex/prepare-repo-for-final-render
      - key: INSTALL_DEV_REQUIREMENTS
        value: "false"

  codex/prepare-repo-for-final-render
      - key: INSTALL_DEV_REQUIREMENTS
        value: "false"

  main
       main
    healthCheckPath: /health
  - type: redis
    name: mba-redis
    plan: starter
    region: oregon

databases:
  - name: mba-postgres-main
    databaseName: masterise_ai_main
    plan: starter
    region: oregon
