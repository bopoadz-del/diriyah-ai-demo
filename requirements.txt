# Core app
fastapi==0.110.0
uvicorn==0.29.0
requests==2.32.3

# OpenAI (new SDK)
openai==1.43.0

# Google OAuth + Drive API
google-auth==2.34.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.141.0
google-auth-httplib2==0.2.0
httplib2==0.22.0

# Document parsing
PyMuPDF==1.24.8        # PDF (fast + supports images)
pypdf==4.3.1           # fallback PDF text extraction
python-docx==1.1.2     # .docx
openpyxl==3.1.5        # .xlsx
pandas==2.2.2          # tables / csv
lxml==5.2.2            # robust XML parsing

# Vector DB (RAG)
chromadb==0.5.5

# --- Optional but Nice ---
numpy==1.26.4          # avoids version conflicts
chardet==5.2.0         # encoding detection (Arabic, etc.)
tqdm==4.66.5           # progress bars
python-multipart==0.0.9 # FastAPI file uploads
xlrd==2.0.1            # old Excel (.xls)
matplotlib==3.9.2      # charts/plots if needed
